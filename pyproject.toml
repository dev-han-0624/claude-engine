[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "claude-engine"
version = "0.1.0"
description = "Claude CLI를 프로세스로 관리하는 독립 엔진 라이브러리"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["engine*"]
